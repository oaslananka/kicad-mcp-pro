{
  "identifier": "default",
  "description": "Default capability — grants core permissions to the main window",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "shell:default",
    "dialog:default"
  ]
}
