# Lists every secret name this repo expects to find injected at CI runtime
# via `doppler run`. Values live only in Doppler project `all`, config `main`.
# Never put values in this file.

CODECOV_TOKEN
DOPPLER_GITHUB_SERVICE_TOKEN
PYPI_TOKEN
SAFETY_API_KEY
TEST_PYPI_TOKEN
